{
	"name": "ignored-files",
	"displayName": "Ignored files",
	"version": "0.0.1",
	"description": "Retrieve list of files ignored/included by Git, NPM, Yarn, JSR, VSCE or other tools.",
	"categories": [
		"Other",
		"SCM Providers"
	],
	"license": "MIT",
	"author": "Mopsgamer",
	"repository": {
		"type": "git",
		"url": "git://github.com/Mopsgamer/ignored-files.git"
	},
	"publisher": "Mopsgamer",
	"type": "module",
	"main": "./dist/extension.cjs",
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "npm run check-types && npm run lint && npm run fmt && node esbuild.js",
		"watch": "npm-run-all -p watch:*",
		"watch:esbuild": "node esbuild.js --watch",
		"watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
		"package": "npm run check-types && npm run lint && node esbuild.js --production",
		"vsix": "vsce package",
		"check-types": "tsgo --noEmit",
		"lint": "oxlint",
		"fmt": "oxfmt"
	},
	"dependencies": {
		"ms": "^2.1.3",
		"view-ignored": "^0.11.1"
	},
	"devDependencies": {
		"@types/ms": "latest",
		"@types/node": "^20.19.41",
		"@types/vscode": "^1.107.0",
		"@typescript/native-preview": "latest",
		"@vscode/test-electron": "^2.5.2",
		"@vscode/vsce": "^3.9.1",
		"esbuild": "latest",
		"npm-run-all": "latest",
		"oxfmt": "latest",
		"oxlint": "latest",
		"typescript": "^6.0.3"
	},
	"contributes": {
		"commands": [
			{
				"title": "Disable decorations",
				"command": "ignoredFiles.scan.clear",
				"category": "Ignored Files",
				"icon": "$(clear-all)"
			},
			{
				"title": "Scan decorations",
				"command": "ignoredFiles.scan",
				"category": "Ignored Files",
				"icon": "$(source-control-view-icon)"
			},
			{
				"title": "Explain ignoring reasons",
				"command": "ignoredFiles.explain",
				"category": "Ignored Files",
				"enablement": "filesExplorerFocus && foldersViewVisible && !explorerResourceIsRoot",
				"icon": "$(question)"
			}
		],
		"menus": {
			"explorer/context": [
				{
					"command": "ignoredFiles.explain",
					"group": "explainignoring",
					"when": "filesExplorerFocus && foldersViewVisible && !explorerResourceIsRoot"
				}
			],
			"view/title": [
				{
					"command": "ignoredFiles.scan.clear",
					"when": "view == workbench.explorer.fileView && ignoredFiles.isScanning",
					"group": "navigation"
				},
				{
					"command": "ignoredFiles.scan",
					"when": "view == workbench.explorer.fileView && ignoredFiles.isReady && !ignoredFiles.isScanning",
					"group": "navigation"
				}
			]
		}
	},
	"activationEvents": [
		"onStartupFinished"
	],
	"engines": {
		"vscode": "^1.107.0"
	}
}
